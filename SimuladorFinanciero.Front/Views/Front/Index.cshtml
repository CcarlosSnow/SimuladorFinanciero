@using SimuladorFinanciero.Entities

@section ScriptHead{
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.2/jquery.min.js"></script>
    @Scripts.Render("~/Front/basejs")
    @Styles.Render("~/Front/basecss")
    <link href="http://fontawesome.io/assets/font-awesome/css/font-awesome.min.css" rel="stylesheet" />
}
<section class="row">
    <div class="center">
        <div class="page a">
            <h1>Simulador Financiero</h1>
            <p class="m">Costos financieros de las principales operaciones de comercio exterior</p>
            <a href="#condiciones" class="link fancybox">Ver consideraciones de uso</a>
            <div class="steps">
                <div class="num active">1</div>
                <div class="line"><div class="load"></div></div>
                <div class="num">2</div>
            </div>
            <div class="box-transition">
                <div id="setp-1">
                    <h2>Seleccione un producto</h2>
                    <div class="accordion">
                        <ul>
                            <li class="a">
                                <a class="title">Medios de pago <i class="fa fa-caret-right" aria-hidden="true"></i></a>
                                <div class="content">
                                    @foreach (Producto i in ViewBag.ListaMediosDePago)
                                    {
                                        <a class="item" href="@Url.Action("Paso2","Front", new { Tipo = 1, IdProducto = i.IdProducto })"><span></span>@i.Nombre.Substring(4)</a>
                                    }
                                   
                                </div>
                            </li>
                            <li class="b">
                                <a class="title">Financiamiento <i class="fa fa-caret-right" aria-hidden="true"></i></a>
                                <div class="content">
                                    @foreach (Producto i in ViewBag.ListaFinanciamiento)
                                    {
                                        <a class="item" href="@Url.Action("Paso2","Front", new { Tipo = 2, IdProducto = i.IdProducto })"><span></span>@i.Nombre.Substring(4)</a>
                                    }
                                </div>
                            </li>
                            <li class="c">
                                <a class="title">Garantías <i class="fa fa-caret-right" aria-hidden="true"></i></a>
                                <div class="content">
                                    @foreach (Producto i in ViewBag.ListaGarantias)
                                    {
                                        <a class="item" href="@Url.Action("Paso2","Front", new { Tipo = 3, IdProducto = i.IdProducto })"><span></span>@i.Nombre.Substring(4)</a>
                                    }
                                </div>
                            </li>
                            <li class="d">
                                <a href="@Url.Action("Paso2","Front", new { Tipo = 4, IdProducto = 0})" class="title">Envío de dinero <i class="fa fa-caret-right" aria-hidden="true"></i></a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<div id="condiciones" class="hidden">
    <h3>Consideraciones de uso</h3>
    <p>La información presentada es referencial. Esta ha sido obtenida de los tarifarios de las entidades bancarias para los principales productos de comercio exterior, así como las empresas administradoras de medios de pago electrónico y envío de dinero; por ello PROMPERU no se responsabiliza por la misma.</p>
    <p>Los resultados pueden variar de acuerdo a la evaluación que las entidades realicen a las empresas al momento de solicitar un producto.</p>
    <p>Puede dejar sus consultas o comentarios <a href="@Url.Action("Contacto","Front")">aquí</a>.</p>
</div>
<div id="medios-de-pago" class="hidden acc">
    <h3>Medios de pago</h3>
    <div class="content">
        @foreach (Producto i in ViewBag.ListaMediosDePago)
        {
            <a class="item" href="@Url.Action("Paso2","Front", new { Tipo = 1, IdProducto = i.IdProducto })"><span></span>@i.Nombre.Substring(4)</a>
        }
    </div>
</div>
<div id="financiamiento" class="hidden acc">
    <h3>Financiamiento</h3>
    <div class="content">
        @foreach (Producto i in ViewBag.ListaFinanciamiento)
        {
            <a class="item" href="@Url.Action("Paso2","Front", new { Tipo = 2, IdProducto = i.IdProducto })"><span></span>@i.Nombre.Substring(4)</a>
        }
    </div>
</div>
<div id="garantias" class="hidden acc">
    <h3>Garantías</h3>
    <div class="content">
        @foreach (Producto i in ViewBag.ListaGarantias)
        {
            <a class="item" href="@Url.Action("Paso2","Front", new { Tipo = 3, IdProducto = i.IdProducto })"><span></span>@i.Nombre.Substring(4)</a>
        }
    </div>
</div>
